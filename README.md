# my-python-learning-lab

学習成果の Python モノレポです。ブランチ運用は `main / develop / feature/*`、コミットは Conventional Commits を採用しています。

## Apps
- apps/RAGChatbot: RAGベースのチャットボット

## How to run (例)
各アプリの README を参照してください。基本は `python -m venv .venv` → `pip install -r requirements.txt` → `pytest` など。
